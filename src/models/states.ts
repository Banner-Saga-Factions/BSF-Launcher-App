export enum loginStates {
    loggedOut,
    loginPending,
    loggedIn,
}

export enum installedStates {
    notInstalled,
    downloading,
    installing,
    installed,
}

export enum updateStates {
    noUpdate,
    checkingForUpdate,
    updateAvailable,
    updateNotAvailable,
}
